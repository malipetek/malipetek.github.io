name: 🚀 Build page

on:
  push:
    branches:
      - "master"

  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: 🎉 Build & deploy
    runs-on: ubuntu-latest
    env:
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      PUBLIC_KEYSTATIC_GITHUB_APP_SLUG: malipetek-dev-keystatic
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v6
    - name: 📦 Install pnpm
      uses: pnpm/action-setup@v6
      with:
        version: 9.4.0
    - name: 📦 Install Node.js
      uses: actions/setup-node@v6
      with:
        node-version: '20.x'
        cache: pnpm

    - name: Install dependencies
      run: pnpm install --frozen-lockfile

    - name: set git email
      run:  git config user.email "support+actions@github.com"

    - name: set git user
      run: git config user.name "Github Actions Bot"

    - name: Build ⚙️
      run: pnpm build
      env:
        CI: false
    
    - name: Deploy to GitHub Pages
      uses: peaceiris/actions-gh-pages@v4
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_branch: gh-pages
        publish_dir: ./dist
        cname: malipetek.dev

    - name: Deploy to Cloudflare Worker
      if: ${{ env.CLOUDFLARE_API_TOKEN != '' }}
      run: pnpm exec wrangler deploy

    - name: Cloudflare deploy skipped
      if: ${{ env.CLOUDFLARE_API_TOKEN == '' }}
      run: echo "::warning::Set the CLOUDFLARE_API_TOKEN repository secret to enable automatic Cloudflare Worker deploys."
